//QuerySelectorAll nos sirve para seleccionar muchas clases que se llaman igual

const card = document.querySelectorAll('.card');
console.log(card);

//No existe el elemento
const noExiste = document.querySelectorAll('.noExistes');
console.log(noExiste);